# AWS credentials (used by MLflow production stack for S3 artifact storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# S3 path for MLflow artifact storage
MLFLOW_DEFAULT_ARTIFACT_ROOT=s3://your-bucket/mlflow-artifacts

# PostgreSQL password for MLflow backend store (production stack)
DB_PASSWORD=change_me
